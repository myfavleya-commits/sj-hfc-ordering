"use client";
import Link from "next/link"; import { FormEvent, useState } from "react"; import { ArrowRight } from "lucide-react"; import { SiteHeader } from "../../components/site-header"; import { useCart } from "../../components/cart-provider"; import { money, settings } from "../../lib/data"; import { checkoutSchema } from "../../lib/validation";
export default function Checkout() { const { items, clear } = useCart(); const [error, setError] = useState(""); const [loading, setLoading] = useState(false); const subtotal = items.reduce((a, i) => a + i.price * i.quantity, 0); async function submit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); setError(""); const parsed = checkoutSchema.safeParse(Object.fromEntries(new FormData(e.currentTarget))); if (!parsed.success) { setError(parsed.error.issues[0].message); return; } setLoading(true); const response = await fetch("/api/orders", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ ...parsed.data, items: items.map((item) => ({ productId: item.id, variantId: item.variantId, quantity: item.quantity })) }) }); const result = await response.json(); if (!response.ok) { setError(result.error || "We could not create your order."); setLoading(false); return; } clear(); window.location.href = `/order/success/${encodeURIComponent(result.order.orderNumber)}?whatsapp=${encodeURIComponent(result.whatsappUrl)}`; }
 if (!items.length) return <><SiteHeader /><main className="container"><div className="success"><h1>Nothing to order yet</h1><p>Add a product to your cart before checkout.</p><Link href="/products" className="btn btn-primary" style={{ marginTop: 20 }}>Browse products</Link></div></main></>; return <><SiteHeader /><main className="container"><div className="page-head"><div className="eyebrow">Almost there</div><h1>Order details.</h1></div><form className="checkout-layout" onSubmit={submit}><section className="panel"><h2>Where should we deliver?</h2><div className="form-grid"><div className="field"><label htmlFor="customerName">Full name</label><input id="customerName" name="customerName" placeholder="Your name" /></div><div className="field"><label htmlFor="phone">Phone number</label><input id="phone" name="phone" placeholder="03001234567" /></div><div className="field full"><label htmlFor="address">Delivery address</label><input id="address" name="address" placeholder="House, street, area" /></div><div className="field"><label htmlFor="city">City</label><input id="city" name="city" placeholder="Lahore" /></div><div className="field"><label htmlFor="landmark">Landmark <span className="muted">(optional)</span></label><input id="landmark" name="landmark" placeholder="Near..." /></div><div className="field full"><label htmlFor="notes">Order notes <span className="muted">(optional)</span></label><textarea id="notes" name="notes" rows={4} placeholder="Anything our team should know?" /></div></div>{error && <p style={{ color: "#b44c3d", marginTop: 16, fontSize: 13 }}>{error}</p>}<button className="btn btn-primary" style={{ marginTop: 22 }} disabled={loading}>{loading ? "Preparing order..." : "Create order & open WhatsApp"} <ArrowRight size={16} /></button></section><aside className="panel"><h2>Order summary</h2>{items.map((i) => <div className="summary-row" key={`${i.id}:${i.variantId||""}`}><span>{i.name}{i.variantName?` (${i.variantName})`:""} × {i.quantity}</span><strong>{money(i.price * i.quantity)}</strong></div>)}<div className="summary-row"><span>Delivery</span><strong>{money(settings.deliveryFee)}</strong></div><div className="summary-row summary-total"><span>Total estimate</span><strong>{money(subtotal + settings.deliveryFee)}</strong></div><p className="muted" style={{ fontSize: 12, lineHeight: 1.5, marginTop: 16 }}>The final price, delivery fee, and stock are verified securely on the server.</p></aside></form></main></>;
}
