"use client";import { useEffect,useState } from "react";import { AdminShell } from "../../../components/admin-shell";import { money } from "../../../lib/data";
 type Order={id:string;orderNumber:string;customerName:string;customerPhone:string;total:number;status:string;createdAt:string};export default function Orders(){const [status,setStatus]=useState("ALL");const [query,setQuery]=useState("");const [orders,setOrders]=useState<Order[]>([]);const [error,setError]=useState("");async function load(){const r=await fetch(`/api/orders?status=${status}&query=${encodeURIComponent(query)}`);if(r.ok)setOrders(await r.json());else setError("Could not load orders.")}useEffect(()=>{load()},[status,query]);return <AdminShell><div className="page-head" style={{paddingTop:0}}><div className="eyebrow">Store operations</div><h1>Orders</h1></div><div className="toolbar"><input className="search" placeholder="Search order or customer..." value={query} onChange={e=>setQuery(e.target.value)}/><select className="filter" value={status} onChange={e=>setStatus(e.target.value)}><option>ALL</option><option>PENDING</option><option>CONFIRMED</option><option>PROCESSING</option><option>SHIPPED</option><option>DELIVERED</option><option>CANCELLED</option></select></div>{error&&<p style={{color:"#b44c3d"}}>{error}</p>}<div className="table-wrap"><table className="table"><thead><tr><th>Order</th><th>Customer</th><th>Phone</th><th>Total</th><th>Status</th><th>View</th></tr></thead><tbody>{orders.map(r=><tr key={r.id}><td>{r.orderNumber}</td><td>{r.customerName}</td><td>{r.customerPhone}</td><td>{money(r.total)}</td><td><span className="badge">{r.status}</span></td><td><a className="btn" href={`/admin/orders/${r.id}`}>View order</a></td></tr>)}</tbody></table></div></AdminShell>}
