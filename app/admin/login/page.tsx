"use client";import { FormEvent,useState } from "react";import { useRouter } from "next/navigation";import Link from "next/link";import { LockKeyhole } from "lucide-react";
export default function AdminLogin(){const router=useRouter();const [error,setError]=useState("");function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);if(f.get("email")==="admin@novamart.demo"&&f.get("password")==="Admin123!"){sessionStorage.setItem("nova-admin","true");router.push("/admin")}else setError("Invalid demo credentials. Try admin@novamart.demo / Admin123!")}return <main style={{minHeight:"100vh",display:"grid",placeItems:"center",padding:20}}><div className="panel" style={{width:"min(420px,100%)"}}><Link href="/" className="brand"><span className="brand-mark">N</span><span>NOVA MART</span></Link><h1 style={{fontSize:32,marginTop:35}}>Welcome back.</h1><p className="muted" style={{margin:"10px 0 25px"}}>Sign in to manage your store.</p><form onSubmit={submit} style={{display:"grid",gap:16}}><div className="field"><label>Email</label><input name="email" type="email" defaultValue="admin@novamart.demo" required/></div><div className="field"><label>Password</label><input name="password" type="password" defaultValue="Admin123!" required/></div>{error&&<p style={{color:"#b44c3d",fontSize:13}}>{error}</p>}<button className="btn btn-primary" type="submit"><LockKeyhole size={16}/> Sign in</button></form><p className="muted" style={{fontSize:12,marginTop:22}}>Development demo account. Change before production.</p></div></main>}
