"use client";import { FormEvent,useState } from "react";import { signIn } from "next-auth/react";import Link from "next/link";import { LockKeyhole } from "lucide-react";
export default function AdminLogin(){const [error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setError("");const f=new FormData(e.currentTarget);const result=await signIn("credentials",{email:f.get("email"),password:f.get("password"),redirect:false});if(result?.error)setError("Invalid email or password.");else window.location.href="/admin"}return <main style={{minHeight:"100vh",display:"grid",placeItems:"center",padding:20}}><div className="panel" style={{width:"min(420px,100%)"}}><Link href="/" className="brand"><span className="brand-mark">SJ</span><span>SJ H.F.C</span></Link><h1 style={{fontSize:32,marginTop:35}}>Welcome back.</h1><p className="muted" style={{margin:"10px 0 25px"}}>Sign in to manage SJ H.F.C.</p><form onSubmit={submit} style={{display:"grid",gap:16}}><div className="field"><label>Email</label><input name="email" type="email" required/></div><div className="field"><label>Password</label><input name="password" type="password" required/></div>{error&&<p style={{color:"#b44c3d",fontSize:13}}>{error}</p>}<button className="btn btn-primary" type="submit"><LockKeyhole size={16}/> Sign in</button></form><p className="muted" style={{fontSize:12,marginTop:22}}>Development account is created by `npm run db:seed`.</p></div></main>}
