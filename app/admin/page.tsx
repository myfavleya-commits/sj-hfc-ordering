import Link from "next/link"; import { ArrowRight } from "lucide-react"; import { AdminShell } from "../../components/admin-shell"; import { prisma } from "../../lib/prisma"; import { money } from "../../lib/data";
export const dynamic = "force-dynamic";
export default async function Dashboard(){const [orders,pending,revenue,products]=await Promise.all([prisma.order.findMany({take:5,orderBy:{createdAt:"desc"},include:{items:true}}),prisma.order.count({where:{status:"PENDING"}}),prisma.order.aggregate({where:{status:{not:"CANCELLED"}},_sum:{total:true}}),prisma.product.count({where:{isActive:true}})]);return <AdminShell><div style={{display:"flex",justifyContent:"space-between",alignItems:"center"}}><div><div className="eyebrow">Store overview</div><h1 style={{fontSize:36}}>Good morning, admin.</h1></div><Link href="/" className="btn">View storefront <ArrowRight size={15}/></Link></div><div className="stat-grid"><div className="stat"><small>Total orders</small><strong>{orders.length}</strong></div><div className="stat"><small>Pending orders</small><strong>{pending}</strong></div><div className="stat"><small>Revenue</small><strong>{money(revenue._sum.total||0)}</strong></div><div className="stat"><small>Products</small><strong>{products}</strong></div></div><div className="panel"><div className="section-head"><h2>Recent orders</h2><Link href="/admin/orders" className="btn">View all</Link></div><div className="table-wrap"><table className="table"><thead><tr><th>Order</th><th>Customer</th><th>Total</th><th>Status</th><th>Date</th></tr></thead><tbody>{orders.map((x)=><tr key={x.id}><td>{x.orderNumber}</td><td>{x.customerName}</td><td>{money(x.total)}</td><td><span className="badge">{x.status}</span></td><td>{x.createdAt.toLocaleDateString()}</td></tr>)}</tbody></table></div></div></AdminShell>}
