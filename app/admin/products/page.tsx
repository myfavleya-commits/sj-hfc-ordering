"use client";import { useEffect,useState } from "react";import Link from "next/link";import { AdminShell } from "../../../components/admin-shell";import { money } from "../../../lib/data";type Product={id:string;name:string;sku:string;price:number;stock:number;isActive:boolean;category:{name:string}};export default function AdminProducts(){const [products,setProducts]=useState<Product[]>([]);useEffect(()=>{fetch("/api/products?includeInactive=true").then(r=>r.json()).then(setProducts)},[]);return <AdminShell><div className="section-head"><div><div className="eyebrow">Catalog management</div><h1>Products</h1></div><Link href="/admin/products/new" className="btn btn-primary">+ Add product</Link></div><div className="table-wrap"><table className="table"><thead><tr><th>Product</th><th>SKU</th><th>Category</th><th>Price</th><th>Stock</th><th>Status</th><th>Manage</th></tr></thead><tbody>{products.map(p=><tr key={p.id}><td><strong>{p.name}</strong></td><td>{p.sku}</td><td>{p.category.name}</td><td>{money(p.price)}</td><td>{p.stock}</td><td><span className="badge">{p.isActive?"Active":"Inactive"}</span></td><td><Link className="btn" href={`/admin/products/${p.id}/edit`}>Edit</Link></td></tr>)}</tbody></table></div></AdminShell>}
