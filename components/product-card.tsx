"use client";import Link from "next/link";import { Plus } from "lucide-react";import { Product } from "../lib/types";import { money } from "../lib/data";import { useCart } from "./cart-provider";
export function ProductCard({product}:{product:Product}){const {add}=useCart();return <article className="product-card"><Link href={`/products/${product.slug}`}><img className="product-image" src={product.image} alt={product.name}/></Link><div className="product-info"><div className="rating">★ {product.rating} <span className="muted">({product.reviews})</span></div><h3><Link href={`/products/${product.slug}`}>{product.name}</Link></h3><p className="muted" style={{fontSize:12}}>{product.category}</p><div className="price-row"><span className="price">{money(product.price)} {product.compareAtPrice&&<span className="old-price">{money(product.compareAtPrice)}</span>}</span><button className="btn btn-primary small-btn" aria-label={`Add ${product.name} to cart`} onClick={()=>add(product)}><Plus size={17}/></button></div></div></article>}
